#pragma once

// Copy this file to secrets.h (gitignored) and fill in your real WiFi
// credentials. secrets.h is never committed to git.

const char* ssid = "YOUR_WIFI_SSID";
const char* password = "YOUR_WIFI_PASSWORD";
