# Yield datasets

`raw/` contains the existing yield and state-weather source files moved from the repository-wide yield dataset location. Keep raw inputs immutable; place cleaned or feature-engineered outputs in `processed/`.
