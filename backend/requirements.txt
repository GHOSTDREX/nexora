# Pinned exact — the ML routers load pre-trained joblib/pickle artifacts at
# import time (see app/ml/*/engine.py, predictor.py); an unpinned install
# can silently pull a newer scikit-learn/numpy/pandas than what trained
# those artifacts and break deserialization at startup. These versions are
# the ones the committed models were last verified against.
fastapi==0.141.1
uvicorn[standard]==0.52.4
sqlalchemy==2.0.52
pydantic[email]==2.13.4
bcrypt==5.0.0
pyjwt==2.13.0
python-dotenv==1.2.3
httpx==0.28.1
scikit-learn==1.9.0
pandas==3.0.5
joblib==1.5.3
numpy==2.5.2
Pillow==12.3.0
anthropic==1.1.0
websockets==17.1
python-multipart==0.0.20

# Paddy disease detection (app/ml/disease_detection) — CPU wheel, pinned
# to match the checkpoint the reference Smart-Farming-AI backend trained
# and verified against (paddy_mobilenetv3_best.pth, strict=True load).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0
torchvision==0.28.0
